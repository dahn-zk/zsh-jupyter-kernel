[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
zsh-jupyter-kernel = { editable = true, path = "./dist/pypi/setuptools" }
    # `editable = true` makes the dependencies specified by setup py to be installed as well

[requires]
python_version = "3.10"
